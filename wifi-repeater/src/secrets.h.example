/* WiFi credentials template — copy to secrets.h and fill in.
 * secrets.h is gitignored; real credentials must never be committed. */
#pragma once

// Upstream router (what HiveRelay repeats)
static const char* STA_SSID = "YOUR_ROUTER_SSID";
static const char* STA_PASS = "YOUR_ROUTER_PASSWORD";

// Local AP (what the queen connects to)
static const char* AP_SSID  = "HiveRelay";
static const char* AP_PASS  = "CHOOSE_AN_AP_PASSWORD";
